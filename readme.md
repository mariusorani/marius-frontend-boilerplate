### Marius's Frontend Boilerplate
Run `npm run dev` for Development and `npm run build` for Production

To change the fonts just replace the fonts in the **fonts** directory and also in the **main.scss**
